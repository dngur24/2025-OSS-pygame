# config.py
import pygame

high_scores = [0, 0, 0, 0, 0]
SCREEN_WIDTH = 1100  # 화면 가로
SCREEN_HEIGHT = 600  # 화면 세로
SCREEN = pygame.display.set_mode((SCREEN_WIDTH,SCREEN_HEIGHT)) # 화면 사이즈 설정

game_speed = 20 # 게임 스피드
